---
name: Provider
route: /components/provider
menu: Components
---

import { PropsTable, Playground } from 'docz'
import Provider from './Provider'

# Provider

This component must be used as a wrapper around the application in order for all Abide UI components to use the expected styles and themes (both a default theme and potentially overrides for other sites).

## Properties

<PropsTable of={Provider} />
